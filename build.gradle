apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "ping-pong"
version = '1.0'
mainClassName = "com.mypackage.App"

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

jar {
    manifest {
        attributes(
            'Main-Class': com.package.App
        )
    }
    baseName = 'my-app'
    version = project.version
}
